<h1>Titulo</h1>
<p>Un texto relatando lo que se encuentra en la sala y lo que hay que hacer</p>
@if(ViewBag.Sala != null){
    <p>@ViewBag.Sala</p>
}
@if(ViewBag.Clave != null){
    <p>@ViewBag.Clave</p>
}
<h4>@ViewBag.Jugador</h4>
<button>Pista 1: botones que dan pistas para adivinar la clave.</button>
<form action='@Url.Action("Juego1")'> 
    <label><input type="text" name="Jugador" placeholder = "Ingrese el jugador" required/></label>
    <input type="submit"/>
</form>
<button>Pista 2: botones que dan pistas para adivinar la clave.</button>

<form action='@Url.Action("Habitacion")'> 
    <label><input type="number" name="sala" placeholder = "ingresa el numero de la sala en la que estas" /></label>
    <label><input type="text" name="clave" placeholder = "ingresa la clave de la sala en la que estas" /></label>
    <input type="submit"/>
</form>

